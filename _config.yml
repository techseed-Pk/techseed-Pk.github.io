domain: techseed.net       # if you want to force HTTPS, specify the domain without the http at the start, e.g. example.com
url: https://techseed.net  # the base hostname and protocol for your site, e.g. http://example.com
baseurl:       # place folder name if the site is served in a subfolder

plugins:
  - jekyll-sitemap
  - jekyll-seo-tag